package it.unibo.studio.moviemagazine.activities;

/**
 * Interface to implement whenever there's need to ignore user back pressures
 */
public interface StickyBackStack {
    void setSticky(boolean sticky);
}
